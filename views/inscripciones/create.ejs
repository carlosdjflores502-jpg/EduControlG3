<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="app-body">
  <div class="app-layout">
    <%- include('../partials/sidebar', { activePage }) %>

    <div class="app-main">
      <%- include('../partials/header') %>

      <main class="content-area">
        <section class="page-heading">
          <div>
            <p class="page-kicker mb-2">Gestion academica</p>
            <h2 class="page-title">Nueva Inscripcion</h2>
            <p class="page-description">Selecciona un alumno activo y un curso activo para registrar la inscripcion.</p>
          </div>
          <a href="/inscripciones" class="btn btn-outline-secondary">Volver</a>
        </section>

        <% if (errors.length > 0) { %>
          <div class="alert alert-warning border-0 shadow-sm mb-4" role="alert">
            <strong>Revisa el formulario:</strong>
            <ul class="mb-0 mt-2">
              <% errors.forEach((error) => { %>
                <li><%= error %></li>
              <% }) %>
            </ul>
          </div>
        <% } %>

        <section class="form-panel">
          <form action="/inscripciones/nueva" method="POST" class="row g-3">
            <%- include('form', { inscripcion, alumnos, cursos, estados, submitText: 'Guardar Inscripcion' }) %>
          </form>
        </section>
      </main>

      <%- include('../partials/footer') %>
    </div>
  </div>
</body>
</html>
